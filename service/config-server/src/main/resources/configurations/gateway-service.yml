spring:
  cloud:
    gateway:
      discovery:
        locator:
          enabled: true
      routes:
        - id: customer-service
          uri: http://localhost:8070
          predicates:
            - Path=/api/customers/**

server:
  port: 8222
