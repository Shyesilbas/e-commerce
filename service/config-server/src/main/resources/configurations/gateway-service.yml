spring:
  cloud:
    gateway:
      discovery:
        locator:
          enabled: true
      routes:
        - id: customer-service
          uri: http://localhost:8070
          predicates:
            - Path=/api/customers/**
        - id: product-service
          uri: http://localhost:8060
          predicates:
            - Path=/api/products/**

server:
  port: 8222
